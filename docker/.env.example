# MindMaster local profile defaults
WEB_PORT=8080
VAULT_PORT=9090

# Connector behavior
MM_QUIET_MODE=true
MM_RATE_LIMIT_PER_MIN=30
MM_BACKOFF_BASE_MS=500
MM_BACKOFF_MAX_MS=8000

# Security / policy mode
MM_POLICY_MODE=hybrid
MM_CRYPTO_PROFILE=hybrid
MM_REQUIRE_EXPLICIT_A2A=false

# MVP scope control
MM_PROVIDER_ALLOWLIST=grok
MM_MAX_HOPS=3
MM_MAX_TOKENS_PER_SESSION=12000
